on: push
name: testing
jobs:
  go-test:
    name: Go Test
    runs-on: ubuntu-latest
    steps:
      - name: setup
        uses: actions/checkout@master
      - name: tests
        uses: shoukoo/golang-pipeline/go1.12/test@master
